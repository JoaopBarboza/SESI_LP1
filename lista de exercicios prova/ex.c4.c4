#include <stdio.h>

  int main(){ 
      
    float kg;  
      
    printf("Digite seu valor em kg para ser transformado em libras: ");
    scanf("%f", &kg);
    
    float libras = kg * 2.205;
    
    printf("Seu valor em kg corresponde a %.2f em libras", libras);

    return 0;
}